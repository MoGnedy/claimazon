export default interface Product{
    title?: string,
    sku?: string,
    image?: string,
    thumbnail?: string,
    price?: number,
    currency?: string,
    description?: string,
    author?: string
}